X   # 1 creer les joueurs 
        X   # 2 le 1er joueur lance les dé
        X   # 3 lire les scores des dés
        X   # 4 afficher les scores des dés 
        X   # 5 le joueur va choisir les dés qu'il garde
            X    # 6 diminuer le nombre de lancer de dé restant 
            X    # 7 compter les nombre dés à relancer 
            X    # 8 relancer les dé à relancer
            X    # 9 le joueur va choisir les dés qu'il garde
            X    # 10 diminuer le nombre de lancer de dé restant
            X    # 11 diminuer le nombre de lancer de dé restant
            X    # 12 compter les nombre dés à relancer 
            X    # 13 relancer les dé à relancer
            X    # 14 le joueur va choisir les dés qu'il garde
            X    # 15 diminuer le nombre de lancer de dé restant
            X    # 16 quand nombre de lancer  = 0 selectionner les dés restants
        X    # 6 lorsque 5 dés ont été selectionnés afficher les scores de tous les dés
        X    # 7 Afficher la grille de résultats
        X    # 8 le joueur selectionne la case qu'il veux remplir
            X    # verifier si la case séléctionnée est accessible
            X    # proposer de remplir avec une croix ou un score
            X    # en cas de score verifier que c'est possible
        X    # 9 calculer le score corespondant a la case
        X    # 10 remplir la case
        X    # 11 diminuer le nombre de jeux restants
            X    # si le nombre de jeu du joueur est = 0 calculer le score final
        X    # 12 passer au 2e joueur
        X    # 13 Afficher le vainqueur